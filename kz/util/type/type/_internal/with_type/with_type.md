| Name                                              | `OfType`   | `UseAccessor` | `UseDefault`             | `UseCollection`    | `UseExclusion`        | `UseUnified`              | `UseStrict`         | Description                                                                                                 |
| ------------------------------------------------- | ---------- | ------------- | ------------------------ | ------------------ | --------------------- | ------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------- |
| Strict update matched collection                  | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsInclude` (default) | `AsUnified`               | `AsStrict`          | In a collection (`Type`), replace `TargetType` keys with `DefaultType`                                      |
| Strict update distributed matched collection      | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsInclude` (default) | `AsDistributed` (default) | `AsStrict`          | In a collection (`Type`), replace keys extending `TargetType` with `DefaultType`                            |
| Loose update matched collection                   | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsInclude` (default) | `AsUnified`               | `AsLoose` (default) | In a collection (`Type`), replace keys with `DefaultType` where `TargetType` extends the key type           |
| Loose update distributed matched collection       | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsInclude` (default) | `AsDistributed` (default) | `AsLoose` (default) | In a collection (`Type`), replace keys with `DefaultType` where `TargetType` extends the key's type         |
| Strict update un-matched collection               | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsExclude`           | `AsUnified`               | `AsStrict`          | In a collection (`Type`), replace keys with `DefaultType`                                                   |
| Strict update distributed un-matched collection   | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsExclude`           | `AsDistributed` (default) | `AsStrict`          | In a collection (`Type`), replace keys not extending `TargetType` with `DefaultType`                        |
| Strict update distributed un-matched collection   | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsExclude`           | `AsUnified`               | `AsLoose` (default) | In a collection (`Type`), replace keys with `DefaultType` where `TargetType` does not extend the key type   |
| Loose update distributed un-matched collection    | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | `Collection`       | `AsExclude`           | `AsDistributed` (default) | `AsLoose` (default) | In a collection (`Type`), replace keys with `DefaultType` where `TargetType` does not extend the key's type |
| Pick matched collection                           | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsInclude` (default) | `AsUnified`               | `AsStrict`          | Create a new collection from `Type` with only `TargetType` keys                                             |
| Idents of matched collection                      | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsInclude` (default) | `AsUnified`               | `AsStrict`          | Get the `TargetType` keys from a collection                                                                 |
| Values of matched collection                      | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsInclude` (default) | `AsUnified`               | `AsStrict`          | Create a union of value types of `TargetType` keys                                                          |
| Pick distributed matched collection               | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsInclude` (default) | `AsDistributed` (default) | `AsStrict`          | Create a new collection from `Type` with keys extending `TargetType`                                        |
| Idents of distributed matched collection          | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsInclude` (default) | `AsDistributed` (default) | `AsStrict`          | Get the keys extending `TargetType` from a collection                                                       |
| Values of distributed matched collection          | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsInclude` (default) | `AsDistributed` (default) | `AsStrict`          | Create a union of value types of keys extending `TargetType`                                                |
| Loose pick matched collection                     | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsInclude` (default) | `AsUnified`               | `AsLoose` (default) | Create a new collection from `Type` with keys where `TargetType` extends the key type                       |
| Idents of loose matched collection                | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsInclude` (default) | `AsUnified`               | `AsLoose` (default) | Get the keys from a collection where `TargetType` extends the key type                                      |
| Values of loose matched collection                | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsInclude` (default) | `AsUnified`               | `AsLoose` (default) | Create a union of value types of keys where `TargetType` extends the key type                               |
| Loose pick distributed matched collection         | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsInclude` (default) | `AsDistributed` (default) | `AsLoose` (default) | Create a new collection from `Type` with keys where `TargetType` extends the key type                       |
| Idents of loose distributed matched collection    | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsInclude` (default) | `AsDistributed` (default) | `AsLoose` (default) | Get the keys from a collection where `TargetType` extends the key type                                      |
| Values of loose distributed matched collection    | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsInclude` (default) | `AsDistributed` (default) | `AsLoose` (default) | Create a union of value types of keys where `TargetType` extends the key type                               |
| Pick distributed un-matched collection            | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsExclude`           | `AsUnified`               | `AsStrict`          | Create a new collection from `Type` with only non-`TargetType` keys                                         |
| Idents of distributed un-matched collection       | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsExclude`           | `AsUnified`               | `AsStrict`          | Get the non-`TargetType` keys from a collection                                                             |
| Values of distributed un-matched collection       | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsExclude`           | `AsUnified`               | `AsStrict`          | Create a union of value types of non-`TargetType` keys                                                      |
| Pick distributed un-matched collection            | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsExclude`           | `AsDistributed` (default) | `AsStrict`          | Create a new collection from `Type` with keys not extending `TargetType`                                    |
| Idents of distributed un-matched collection       | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsExclude`           | `AsDistributed` (default) | `AsStrict`          | Get the keys not extending `TargetType` from a collection                                                   |
| Values of distributed un-matched collection       | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsExclude`           | `AsDistributed` (default) | `AsStrict`          | Create a union of value types of keys not extending `TargetType`                                            |
| Loose pick un-matched collection                  | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsExclude`           | `AsUnified`               | `AsLoose` (default) | Create a new collection from `Type` with keys where `TargetType` does not extend the key type               |
| Idents of loose un-matched collection             | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsExclude`           | `AsUnified`               | `AsLoose` (default) | Get the keys from a collection where `TargetType` does not extend the key type                              |
| Values of loose un-matched collection             | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsExclude`           | `AsUnified`               | `AsLoose` (default) | Create a union of value types of keys where `TargetType` does not extend the key type                       |
| Loose pick distributed un-matched collection      | `Of<Type>` | `AsGetter`    | None                     | `Collection`       | `AsExclude`           | `AsDistributed` (default) | `AsLoose` (default) | Create a new collection from `Type` with keys where `TargetType` does not extend the key type               |
| Idents of loose distributed un-matched collection | `Of<Type>` | `AsGetter`    | None                     | `CollectionIdents` | `AsExclude`           | `AsDistributed` (default) | `AsLoose` (default) | Get the keys from a collection where `TargetType` does not extend the key type                              |
| Values of loose distributed un-matched collection | `Of<Type>` | `AsGetter`    | None                     | `CollectionValues` | `AsExclude`           | `AsDistributed` (default) | `AsLoose` (default) | Create a union of value types of keys where `TargetType` does not extend the key type                       |
| Extract union                                     | `Of<Type>` | `AsGetter`    | None                     | None               | `AsInclude` (default) | None                      | None                | Extract `TargetType` from `Type`                                                                            |
| Exclude union                                     | `Of<Type>` | `AsGetter`    | None                     | None               | `AsExclude`           | None                      | None                | Remove `TargetType` from `Type`                                                                             |
| Intersection                                      | `Of<Type>` | `AsSetter`    | None                     | None               | None                  | `AsUnified`               | None                | Creates an intersection of `TargetType` and `Type`                                                          |
| Unionize                                          | `Of<Type>` | `AsSetter`    | None                     | None               | None                  | `AsDistributed` (default) | None                | Replace any of `TargetType` in `Type` with `DefaultType`                                                    |
| Strict type replacement                           | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | None               | None                  | `AsUnified`               | None                | Replace `TargetType` in `Type` with `DefaultType`                                                           |
| All type replacement                              | `Of<Type>` | `AsSetter`    | `DefaultOf<DefaultType>` | None               | None                  | `AsDistributed` (default) | None                | Replace any of `TargetType` in `Type` with `DefaultType`                                                    |
| Default                                           | None       | None          | None                     | None               | None                  | None                      | None                | Returns `TargetType`                                                                                        |
