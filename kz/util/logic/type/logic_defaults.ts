import type { $AsCondition } from '@kz/util/capability';

/**
 * The default capability settings for logic operations.
 *
 * @example //TODO(@ebntly): Add an example
 *
 * ## Default Capability Settings
 * | Capability | Default Setting |
 * |------------|-----------------|
 * | {@linkcode $UseCondition} | {@linkcode $AsCondition} |
 */
export type LogicDefaults = $AsCondition;
